export interface IItemDescription{
    title: string;
    text: string;
    icons: string[];
}

export interface IInfoCard{
    title: string;
    text: string;
    images?: Array<string>;
    colorButton?: string;
    textButton?: string;
}